var e='# 網路概念筆記：DNS 與 NetworkManager\n\n**來源**：從 Kali VM 的 DNS 排查案例延伸出的問答整理（對應 Linux 學習計畫階段二 - 網路基礎的先修內容）\n\n## 1. nameserver 一定要填 IP，不能填網域名稱\n\n`/etc/resolv.conf` 裡的 `nameserver` 欄位**必須是 DNS 伺服器的 IP 位址**，不能直接寫網域名稱（例如不能寫 `actx.lab`）。\n\n**原因**：DNS 的工作就是「把名稱換成 IP」。如果 `nameserver` 本身填的是一個名稱，系統要先查出這個名稱對應的 IP 才能去問它——但都還沒有能用的 DNS 伺服器，就沒辦法查任何名稱，形成「先有雞還是先有蛋」的死結。所以一定要先給系統一個**已知的 IP**，讓它能直接連上去問。\n\n## 2. `search` 網域後綴 跟 `nameserver` 是兩回事\n\n以 `/etc/resolv.conf` 裡原本的內容為例：\n\n```\nsearch actx.lab\n```\n\n這一行不是 DNS 伺服器位址，而是**搜尋網域後綴**。作用是：當你打一個「短名稱」（例如 `kali01`，不是完整網域名稱）時，系統會自動幫你補上 `.actx.lab`，變成 `kali01.actx.lab` 再去查。\n\n`search` 決定「怎麼補全名稱」，`nameserver` 決定「去問誰」，兩者要同時存在才能正常運作，缺一個都不行。\n\n## 3. 為什麼內部網域（AD）需要指向自己的 DNS 伺服器\n\n如果 lab 網路裡有一台**自建的 Windows AD**（Active Directory Domain Services），這台主機通常會**同時擔任 DNS Server**。原因是 AD 本身依賴 DNS 來運作：網域內的服務（例如 LDAP、Kerberos）是透過一種叫 **SRV record** 的 DNS 紀錄被其他機器找到的。\n\n公用 DNS（如 1.1.1.1、8.8.8.8）完全不知道 `actx.lab` 這個內部網域的存在，只認識公開網際網路上的網域（如 kali.org）。所以：\n\n- 要解析 `actx.lab` 內部資源 → 必須指向 AD 主機的 IP\n- 要解析外部網址（如 kali.org） → 可以用公用 DNS，**或**由 AD 主機轉發出去（見下一節）\n\n## 4. DNS 轉發器（Forwarders）決定要不要加公用 DNS 備援\n\nAD 的 DNS 伺服器可以設定「轉發器」，把它自己解不出來的查詢（外部網址）轉去問公用 DNS。這樣一來，client 只需要指向 AD 主機一個 DNS 就好，內部、外部都通。\n\n**測試方法**：\n\n```bash\nnslookup actx.lab\nnslookup kali.org\n```\n\n- 兩個都查得到 → AD DNS 的轉發器設定完整，只填它一個即可\n- 只有內部網域查得到、外部查不到 → 代表沒有轉發器，這時要把公用 DNS 也加進去當備援：\n\n```bash\nsudo nmcli connection modify "Wired connection 1" ipv4.dns "<AD主機的IP> 1.1.1.1"\nsudo nmcli connection up "Wired connection 1"\n```\n\n## 5. `"Wired connection 1"` 是什麼——裝置 vs 連線設定檔\n\n`eth0` 和 `Wired connection 1` 是兩個不同層次的東西：\n\n| 名稱 | 是什麼 |\n|------|--------|\n| `eth0` | **裝置（Device）**，實體/虛擬網卡本身，由 Linux 核心命名，通常不會變 |\n| `Wired connection 1` | **連線設定檔（Connection Profile）**，NetworkManager 管理的一組設定值（IP、閘道、DNS），套用在某個裝置上 |\n\n會分開設計，是因為同一張網卡理論上可以套用不同的設定檔（例如 Wi-Fi 網卡可能有多個基地台的設定檔），有線網路通常只有一組，NetworkManager 沒特別命名的話，預設就叫 `Wired connection 1`。\n\n**驗證指令**：\n\n```bash\nnmcli connection show\n```\n\n輸出欄位意思：\n\n| 欄位 | 意思 |\n|------|------|\n| `NAME` | 連線設定檔的名字 |\n| `UUID` | NetworkManager 給這組設定檔的唯一識別碼 |\n| `TYPE` | 連線類型（例如 `ethernet`） |\n| `DEVICE` | 目前套用在哪個裝置上 |\n\n實際跑出的結果確認：`Wired connection 1` 對應 `eth0`，跟前面下 `nmcli connection modify "Wired connection 1" ...` 時用的名字一致，設定都是修改在正確的地方。\n';export{e as default};